language: python
python:
  - "2.7"
cache:
  directories:
    - $HOME/.cache/pip
services: mongodb
install:
  - "pip install twill --allow-external twill --allow-unverified twill"
  - "pip install -r requirements_test.txt"
script: make test
after_success:
  - coveralls